#!/usr/bin/env zsh

# configre screens
rp-screens startup

# autostart
autorandr -c
nitrogen --restore &

# start st with tmux
# attach, if session exists, create a new one else
st -c "terminal" -t "terminal" -e ~/bin/tm-aon &

ratpoison
