# ZLE widget run-last-with-sudo
# replace current command line with sudo !!
# save the current line in the buffer stack
if [[ $LBUFFER == 'sudo !!' ]]
then
  LBUFFER=''
  zle .get-line
else
  zle .push-input
  LBUFFER='sudo !!'
fi
