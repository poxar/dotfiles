# ZLE widget grml-zsh-fg
# quickly put job to foreground
# from grml.org
if (( ${#jobstates} )); then
  zle .push-input
  [[ -o hist_ignore_space ]] && BUFFER=' ' || BUFFER=''
  BUFFER="${BUFFER}fg"
  zle .accept-line
else
  zle -M 'No background jobs. Doing nothing.'
fi
