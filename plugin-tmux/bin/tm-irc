#!/usr/bin/env zsh

if [[ -z $TMUX ]]; then
  tm-init
fi

tmux new-window "mosh pmi@aerelon.de -- tmux attach -t irc"
tmux split-window -h "irssi --home ~/.irssi-bitlbee/"
tmux rename-window irc
tmux move-window -t 0
