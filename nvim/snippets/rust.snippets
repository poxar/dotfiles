snippet fn
	${2}fn ${1:name}(${3})${4} {
		${0:${VISUAL}}
	}
snippet for
	for ${1:needle} in ${2:haystack} {
		${0:${VISUAL}}
	}
snippet match
	match ${1:option} {
		Some(${2:value}) => ${3:do_something(value)},
		None => ${4:handle_none()}
	}${0}
