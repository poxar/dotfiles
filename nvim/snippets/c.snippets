snippet for
	for (int ${1:i} = 0; $1 < ${2:condition}; ++$1) {
		${0}
	}
snippet inc
	#include $0
snippet once
	#ifndef ${1:`substitute(toupper(expand('%:t')), '\.', '_', 'g')`}
	#define $1

	${0}

	#endif /* $1 */
