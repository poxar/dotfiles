snippet fn
	${1:private} function ${2:name}($3)$4 {
		${0:${VISUAL}}
	}
snippet cl
	class ${1:Name} extends ${2:Parent}$3 {

		public function __construct($4) {
			parent::__construct($5);$6
		}

		${0:${VISUAL}}

	}
snippet \l
	function($1) use($2) {
		${0:${VISUAL}}
	}
snippet for
	foreach ($${1:variable} as $${2:value}) {
		${0:${VISUAL}}
	}
snippet fori
	for ($${2:i} = 0; $$2 < ${1:count}; $$2${3:++}) {
		${0:${VISUAL}}
	}
