#!/usr/bin/env zsh

# configre screens
rp-screens startup

# autostart
xcompmgr &
nitrogen --restore &
pgrep dropbox &>/dev/null || dropboxd &
nm-applet &

# start xterm with tmux
# attach, if session exists, create a new one else
xterm -title "xterm" -e tm-aon &

ratpoison
