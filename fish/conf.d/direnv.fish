# Simple node version manager
# Find your version here: https://nodejs.org/en/download/releases/
# Download required version and unpack to $HOME/Code/node
# Add `use node SOMEVERSION` to .envrc
# Profit :)
set -x NODE_VERSIONS $HOME/Code/node
