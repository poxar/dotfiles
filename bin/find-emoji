#!/bin/sh
set -eu

FILE="${XDG_CONFIG_HOME:-"$HOME/.config"}/emoji.txt"

emoji="$(cat "$FILE" | sk | cut -d ' ' -f 1 | tr -d '\n')"
wl-copy "${emoji}"

if test "${1:-""}" = "type"; then
  keyd "do" A-esc C-v
else
  keyd "do" C-V
fi

exit
